<h4>Edit Card: <%= @wallet.cardnumber %></h4>
<%= form_for :wallet, :url => user_wallet_path(@user, @wallet), method: :patch do |f| %>
  <div class="field">
    <b><%= f.label :Name_on_Card %><br /> </b>
  </div>
  <%= f.text_field :cardowner, autofocus: true %>
  <div class="field">
    <b><%= f.label :Card_Number %><br /> </b>
    <%= f.text_field :cardnumber, autofocus: true %>
  </div>
  <br/>
  <div class="field">
    <b><%= f.label :Expire_Date %><br /> </b>
    <%= f.date_field :expiredate, autofocus: true %>
  </div>
  <br/>
  <div class="actions">
    <b> <%= f.submit "Update" %> </b>
  </div>
<% end %>

<h3>Delete Card</h3>
  <%= button_to "Delete Card", user_wallet_path(@user, @wallet), data: { confirm: "Are you sure?" }, method: :delete %></p>

<%= link_to 'Back', user_wallets_path %>
